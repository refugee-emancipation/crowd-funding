language: ruby
rvm:
- 2.1

env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true # speeds up installation of html-proofer

before_install:
  - gem install html-proofer

script:
  - bundle exec jekyll build
  - htmlproof build --disable-external --check-html --alt-ignore ".*piwik.*"
